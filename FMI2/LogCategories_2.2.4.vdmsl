/**
 * 2.2.4 Log Categories
 */
types
	Category ::
		name			: NormalizedString1
		description		: [AnyString];

functions
	isValidLogCategories: [seq1 of Category] +> bool
	isValidLogCategories(cats) ==
		cats <> nil =>
			-- @OnFail("LogCategory names are not unique")
			( card { c.name | c in seq cats } = len cats );